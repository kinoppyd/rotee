<h1>New item</h1>

<%= turbo_frame_tag "new_list_#{@item.list.id}_item" do %>
  <%= render "form", list: @list, item: @item %>
<% end %>

<br>

<div>
  <%= link_to "Back to items", list_items_path %>
</div>

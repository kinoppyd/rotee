<h1>New item</h1>

<%= turbo_frame_tag "new_list_item_#{@item.list.id}" do %>
  <%= render "form", list: @list, item: @item %>
<% end %>

<br>

<div>
  <%= link_to "Back to items", list_items_path %>
</div>
